# Patch layer for the @wangjunjian/dsh-artifact-viewer bundle.
# Inserts one row that registers the artifact-viewer plugin.

- insert:
    - id: artifact-viewer
      name: '@wangjunjian/dsh-artifact-viewer'
