# Patch layer for the @wang-junjian/dsh-artifact-viewer bundle.
# Inserts one row that registers the artifact-viewer plugin.

- insert:
    - id: artifact-viewer
      name: '@wang-junjian/dsh-artifact-viewer'
